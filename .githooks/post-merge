#!/bin/sh
# Auto-update the installed CLAP/VST3 bundles after every pull/merge, in the
# background. Log: target/plugin-install.log
# Enabled via: git config core.hooksPath .githooks
mkdir -p target
nohup scripts/install-plugins.sh >> target/plugin-install.log 2>&1 &
exit 0
